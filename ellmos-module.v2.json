{
  "schema": "ellmos.module.v2",
  "id": "policy-registry",
  "version": "0.2.0",
  "display_name": "policy-registry",
  "category": "control",
  "kind": "library",
  "status": "active",
  "visibility": "public",
  "provides": [
    "policy.registry",
    "policy.resolve",
    "policy.discovery",
    "delegation.candidate.resolve",
    "decision.location.pointers",
    "rule.append-only",
    "rule.audit-fields",
    "authority.mode-switch",
    "byum.decision-candidate.pointer"
  ],
  "requires": [
    "decision.clicker"
  ],
  "optional": [
    "sync.yard",
    "tom.advisory",
    "mcp",
    "byum.decision-prediction"
  ],
  "conflicts": [],
  "surfaces": [
    "library",
    "cli",
    "mcp-adapter"
  ],
  "state": {
    "ownership": "module",
    "location": "user-home"
  },
  "boundaries": {
    "network": "none",
    "data": "user-local",
    "platforms": [
      "windows",
      "macos",
      "linux"
    ]
  },
  "source_of_truth": {
    "type": "git-repository",
    "path": ".",
    "repository": "https://github.com/ellmos-ai/policy-registry"
  },
  "adapters": [
    {
      "type": "seam",
      "id": "sync-policies-v1",
      "target": ".SYNC/_policies",
      "status": "optional",
      "note": "Imports existing metadata pointers and exports a non-authoritative aggregated view."
    },
    {
      "type": "seam",
      "id": "system-gap-optional",
      "target": "system-gap-master",
      "status": "optional",
      "note": "Transport seam only; the local registry remains authoritative."
    },
    {
      "type": "seam",
      "id": "decision-clicker-v1",
      "target": "decision-clicker",
      "status": "required",
      "note": "Fixed decision-system bundle relationship by user decision on 2026-08-27, superseding the optional composition choice from T-20260824-474639761: the registry requires decision.clicker as its human writer/UI component. The Python availability probe remains guarded because composition and runtime import coupling are separate; both modules continue to share the on-disk _DECISIONS chain without importing each other's internals."
    },
    {
      "type": "seam",
      "id": "byum-pointer-v1",
      "target": "build-your-users-mind",
      "status": "optional",
      "note": "Consumes only a prevalidated BYUM v2 pointer envelope and projects non-authoritative decision-candidate metadata. No BYUM import, event parsing, raw content, adoption, execution, or receipt authority."
    },
    {
      "type": "mcp",
      "id": "policy-registry"
    }
  ]
}
